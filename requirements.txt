# Core dependencies
google-api-python-client>=2.100.0
httplib2>=0.22.0
retry>=0.9.2
requests>=2.31.0
youtube-transcript-api==1.1.1
python-dotenv
PySocks
pydub

# API dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0

# HTTP client for publish service
aiohttp>=3.9.0

# File upload support
python-multipart>=0.0.6

# Additional dependencies for pipeline
aiofiles>=23.0.0

# For Windows compatibility
pywin32>=305 ; sys_platform == 'win32'

# Authentication dependencies
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# Timezone support
pytz>=2024.1

# Cron expression support for scheduling
croniter>=2.0.0

# CORS support (already included in fastapi)
# python-multipart is already listed above